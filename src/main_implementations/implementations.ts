// list all available implementations of main and return a default of in-memory

class MemoryImplementation {
    // return the main memory implementation for use with api endpoints
}
export {
    MemoryImplementation
}